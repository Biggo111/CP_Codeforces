#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,x,y,z;
    int flag=0;
    cin>>n; //3
    while(n--) //3 2 1
    {
        cin>>x>>y>>z;
        int a = abs(x);
        int b = abs(y);
        int c = abs(z);
    }
    int d = a - b;
    if(d==c)
    {
        flag=1;
        //break;
    }
    else
    {
        flag = 0;
        //break;
    }
}
